# Use strict-peer-dependencies=false to avoid peer dependency issues
strict-peer-dependencies=false
# Use legacy-peer-deps=true for compatibility with older packages
legacy-peer-deps=true
# Ensure node_modules are properly linked
node-linker=hoisted
